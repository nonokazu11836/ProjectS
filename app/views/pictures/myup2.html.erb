<h3>自分が投稿した画像</h3>

<div class="flex-4">
  <% @pictures.each do |picture| %>
    <%= link_to image_tag("#{picture.place}") , pictures_myupdetail_path(picture_id: "#{picture.id}") %>
  <% end %>
</div>

<div class="over">
  <%= link_to ("/pictures/studenthome") do %>
    <button> 　　ホームに戻る 　　</button>
  <% end %><br>
</div>